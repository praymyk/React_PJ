import DefaultContent from '@components/palace/test/ai-case-notes/DefaultContent';

// TODO: 임시 company_id 추후 바로보는 카테고리 주체 정의 필요
const DEFAULT_COMPANY_ID = 1;

export default async function Page() {

    return (
        <DefaultContent
        />
    );
}